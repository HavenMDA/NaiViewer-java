# NaiViewer-java
A Java version of NaiViewer, with support for INT, Generations, Deficient, and BSFKL rules.

To compile the program, run

`javac NaiViewer.java`

inside the program’s directory and run

`java NaiViewer`

to run it.

Note that this only works if you have Java installed.
